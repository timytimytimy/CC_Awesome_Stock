# TODO

## P0: 数据层可信度

- [x] 修复 `data/stock_data/company.py` 的 `get_valuation()`。
  - 现象：`get_valuation("600519.SH")` 返回 `stock_a_ttm_lyr() got an unexpected keyword argument 'symbol'`。
  - 期望：脚本能稳定输出 PE/PB 及历史分位；接口不可用时明确降级并保留可审计错误信息。

- [x] 修复 `data/stock_data/company.py` 的 `get_announcements()`。
  - 现象：`get_announcements("600519.SH")` 返回空，并输出 `[WARN] announcements 600519.SH: '600519'`。
  - 期望：能拿到最近公告；拿不到时区分“接口失败”“无公告”“参数格式不匹配”。

- [ ] 补齐数据脚本的最小回归测试。
  - 位置：`tests/data/`。
  - 覆盖：估值、公告、K 线、行业筛选、异常降级路径。
  - 目标：避免 akshare 接口变化后报告静默失真。

## P1: 指标实现一致性

- [x] 修正 `data/stock_data/technical.py` 的 `compute_rps()`。
  - 现状：函数签名包含 `benchmark`，文档写“个股涨幅 vs 基准涨幅”，但实现只计算个股自身区间涨幅。
  - 期望：要么真正计算相对基准强弱，要么改名/改文档，避免报告误读。

- [ ] 梳理 `snapshot_market.py` 与 playbook 的指标差距。
  - 现状：`kb/playbooks/market-regime.md` 提到封板率、成交额结构、风格指数等，脚本并未全部提供。
  - 期望：缺失项要么补数据，要么在报告中显式标为数据缺失。

## P2: Backtest 骨架

- [x] 修复 `backtest/strategies/_template.py` 的过期导入。
  - 现状：模板引用不存在的 `get_index_data` 和 `get_financials`。
  - 期望：模板可直接导入，作为新策略起点不报错。

- [ ] 定义第一版回测公共接口。
  - 明确策略如何读取 `data/` 数据、输出 signals、生成 `metrics.json` 和 `report.md`。

## P3: 文档与仓库卫生

- [x] 更新 `README.md` 的 MVP 状态。
  - 现状：README 写作者档案 3 个，但 `kb/authors/_index.md` 已列 11 人。
  - 结果：README 已更新为作者档案 11 个、散户实践者档案 10 个，并补充 `.agents/skills/a-stock-analyst/` 入口说明。

- [x] 决定是否提交 `.agents/skills/a-stock-analyst/`。
  - 现状：`.agents/` 未跟踪，内容与 `.claude/skills/a-stock-analyst/` 一致。
  - 结果：已纳入版本管理，作为 Codex/Agents 入口。

- [x] 调整报告产物的忽略规则。
  - 现状：`reports/*.md` 被忽略，但 `reports/*.pdf` 没被忽略。
  - 结果：`.gitignore` 已补充 `reports/*.pdf`，报告产物默认不进 git。

- [x] 清理 `.DS_Store` 和编译缓存。
  - 现状：`.DS_Store` 未跟踪，`__pycache__/` 已被忽略但本地存在。
  - 结果：`.DS_Store` 已加入 `.gitignore`，缓存继续不进入版本管理。

## P4: 报告质量护栏

- [ ] 给 `reports/2026-05-20.md` 的生成流程加执行证据。
  - 现状：报告中出现“知识截止日期”类说明，和 skill 的强制搜索义务相冲突。
  - 期望：周报能保留数据脚本输出摘要、搜索来源摘要和失败降级项。

- [ ] 在 `scripts/weekly_analysis.sh` 中保存 stderr 或运行日志。
  - 现状：Claude 调用的 stderr 被丢弃到 `/dev/null`。
  - 期望：失败时能回溯是数据脚本、搜索、模型输出还是 PDF 生成问题。

## P5: 选股分析框架优化

- [ ] 引入单股 `thesis card`。
  - 目标：把每只股票拆成“候选池理由 / 证据表 / 决策表”，避免报告把事实、推断、假设和结论混在一起。
  - 输出：每只候选股都有核心 thesis、证据来源、反对意见、触发条件、失效条件、跟踪事件。

- [ ] 增加“核心假设生命周期”。
  - 目标：每条 `[假设]` 必须绑定验证事件、验证日期、通过标准、失败标准和失败后动作。
  - 输出：报告不只说“逻辑成立”，还说明未来如何证明它对或错。

- [ ] 将一部分风险检查前置到阶段 3。
  - 目标：在深度分析前先过滤明显不该进入候选池的标的。
  - 硬排除项：ST / 监管处罚 / 财务造假 / 高质押或密集减持 / 现金流长期差于利润 / 主题收入占比过低 / 流动性太差。

- [ ] 增加组合层约束。
  - 目标：从“单股是否值得看”升级到“放进组合是否合适”。
  - 输出：单票最大仓位、单行业最大仓位、主题相关性、现金比例、市场状态对应仓位上限。

- [ ] 把市场状态升级为 `regime matrix`。
  - 目标：明确 A/B/C/D 市场状态下哪些策略可用、哪些策略禁用。
  - 示例：A 允许趋势成长和主线资金；B 优先龙头中线和事件验证；C 暂停新仓；D 只允许极小仓逆向且必须有安全边际。

- [ ] 为 backtest 增加防过拟合护栏。
  - 目标：回测不只看收益率最高，而是先定义假设和规则，再做滚动/样本外验证。
  - 输出：记录参数搜索次数、样本内/样本外区间、交易成本、换手率、最大回撤、结论稳定性。

- [ ] 增加结论置信度校准。
  - 目标：记录每次预测的置信度、验证周期和最终结果，长期统计哪些流派/数据/市场状态下判断更可靠。
  - 输出：可复盘的 `prediction log`，用于校准 `low/mid/high`。

- [ ] 增加机会成本对照。
  - 目标：每个候选股必须回答“为什么是它，而不是行业 ETF、行业龙头，或者继续等待？”
  - 输出：报告中新增替代方案比较，避免为了选股而选股。

## P6: 散户实践者层后续补全

- [ ] 扩充 `kb/retail-practitioners/` 的候选池。
  - 优先方向：A 股财报拆解型博主、可转债/低风险套利实践者、长期公开复盘型普通投资者。
  - 准入前必须按 `kb/retail-practitioners/source-quality.md` 评分。

- [ ] 给散户实践者档案补充更完整的来源样本。
  - 当前初始档案只用于建立结构和低/中权重示范。
  - 后续每个档案至少补 3-5 篇代表内容和 1 个失败/不适用场景。

- [ ] 对 `watchlist` 来源做季度复核。
  - 当前 watchlist 包括：普舒、KK爹、TradesMax、勃勃OC。
  - 目标：确认是否仍可作为观察样本，或应转入反面案例/移出索引。

- [ ] 为报告生成增加“散户可执行性”自动检查。
  - 输出：盯盘需求、资金规模、流动性、心理承受力、ETF/基金替代方案。

- [ ] 建立 `prediction log`，记录引用散户实践者后的结论是否更可执行。
  - 目标：验证这层是否真的改善决策质量，而不是增加噪音。

## P7: 候选股持续追踪系统

- [ ] 建立本地 `watchlist` 数据结构。
  - 目标：把每次报告里的 A/B/C 档候选沉淀成可追踪对象，而不是停留在一次性 PDF。
  - 字段：股票代码、股票名称、入池日期、来源报告、初始档位、当前档位、核心假设、触发条件、失败条件、参考支撑/止损、仓位上限、下次复核日期。

- [ ] 将报告中的“核心假设生命周期”落到可更新状态机。
  - 状态：`watching` / `triggered` / `active_trial` / `downgraded` / `removed` / `validated`。
  - 目标：每只股票都能回答“为什么还在看、什么时候可以买一点、什么时候必须移出”。

- [ ] 实现每日/每周追踪任务。
  - 每日：检查价格是否触发支撑、突破、止损、放量回落、重大公告或新闻。
  - 每周：重算市场状态、行业强度、候选评分和档位变化。
  - 输出：只报告“有变化/触发”的项目；没有变化时给简短摘要。

- [ ] 增加触发器和提醒规则。
  - 买入观察触发：回踩关键均线企稳、放量突破、公告/订单/财报验证。
  - 降级触发：跌破关键位无法收回、负面公告、业绩不及预期、行业主线退潮、估值/拥挤度恶化。
  - 移出触发：核心假设证伪、连续两次复核未达标、替代方案明显更优。

- [ ] 生成持续追踪报告。
  - 输出位置：`reports/tracking/`。
  - 输出格式：本日变化、档位调整、触发器命中、需要人工确认的问题、下一步动作。
  - 约束：仍需标注 `[事实] / [推断] / [假设] / [情绪] / [传闻]`，并保留数据时间戳。

- [ ] 给追踪系统接入自动化。
  - 目标：后续可用 Codex automation 定期运行。
  - 初始建议：交易日收盘后跑一次轻量追踪；周末跑一次深度复核。
  - 注意：自动化只做研究和提醒，不自动下单，不输出“立即买入”。

- [ ] 把追踪结果反写到 `prediction log`。
  - 目标：长期统计每次 A/B/C 档判断的命中率、平均回撤、触发后表现、失败原因。
  - 输出：用于校准评分规则和 `strong watch / small trial / track event` 档位。
